[package]
name = "osv-gtk-gallery-prototype"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
gtk = { package = "gtk4", version = "0.11.4", features = ["v4_12"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
